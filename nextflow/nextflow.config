params {
    reference = null
    query = null
    outdir = "results"
    aligner = "mummer"
    min_sv_size = 50
    max_sv_size = 100000
}

process {
    container = "macbio/variant-caller:0.1.0"
}

docker {
    enabled = true
}
